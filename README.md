# XCHAINGER.trade public deploy artifact

This public repository contains only the already-built static website files required by browsers plus a minimal GitHub Pages deployment workflow. Source project files are kept private.
